{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "bootstrap": "lerna clean -y && lerna bootstrap",
    "build": "lerna run build",
    "dev:h5": "lerna exec --scope portal yarn dev:h5"
  },
  "devDependencies": {
    "@babel/cli": "^7.17.10",
    "@babel/core": "^7.17.12",
    "@babel/node": "^7.17.10",
    "@babel/preset-env": "^7.17.12",
    "babel-preset-minify": "^0.5.2",
    "lerna": "^4.0.0"
  }
}
